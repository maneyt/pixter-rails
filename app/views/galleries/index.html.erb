<h2> Galleries </h2>
<% if signed_in? %>
  <%= link_to "Create a gallery", "/galleries/new" %>
<% end %>

<ul>
  <% @galleries.each do |gallery| %>
    <li><%= link_to gallery.name, gallery %> </li>
  <% end %>
</ul>
