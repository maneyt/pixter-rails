<h3> Edit Image </h3>
<%= form_for [@gallery, @image] do |form| %>
  <div>
    <%= form.label :url, "URL" %> 
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.submit "Update Image" %>
  </div>
<% end %>
